# Local runtime/config artifacts
.pi-notify/
node_modules/
package-lock.json
# Sender package must commit its exact @getpaseo/client pin lockfile.
!linux/paseo-sender/package-lock.json
*.log
*.tmp
.DS_Store
Thumbs.db
# Local secret config; keep only the example in git.
linux/remote-windows-notify.json
