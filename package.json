{
  "name": "pi-remote-windows-notify",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pi extension that notifies Windows when remote Pi finishes a turn or waits for an answer.",
  "keywords": [
    "pi-package",
    "pi-extension"
  ],
  "scripts": {
    "check": "node scripts/run-paseo-sender-gate.mjs check && node scripts/check-package.mjs",
    "test": "node scripts/run-paseo-sender-gate.mjs test && node --experimental-transform-types --test test/*.test.mjs",
    "test:paseo-sender": "npm --prefix linux/paseo-sender test",
    "check:paseo-sender": "npm --prefix linux/paseo-sender run check"
  },
  "pi": {
    "extensions": [
      "./linux/extensions/remote-windows-notify.ts"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*"
  }
}
